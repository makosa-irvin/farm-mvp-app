import { useEffect, useRef, useState } from 'react'; import { RefreshCw, MoreHorizontal, Search } from 'lucide-react'; import NavTabs from './NavTabs.jsx'; import styles from './Header.module.css';
export default function Header({ tabs, activeTab, onSelectTab }) { const mobilePrimary = tabs.filter((t) => ['dashboard', 'log', 'inventory', 'expenses'].includes(t.value)); const mobileMore = tabs.filter((t) => ['units', 'suppliers', 'analytics', 'insights', 'reports', 'settings'].includes(t.value)); const [isMenuOpen, setIsMenuOpen] = useState(false); const menuRef = useRef(null); useEffect(() => { if (!isMenuOpen) return undefined; const outside = (e) => { if (menuRef.current && !menuRef.current.contains(e.target)) setIsMenuOpen(false); }; const key = (e) => { if (e.key === 'Escape') setIsMenuOpen(false); }; document.addEventListener('mousedown', outside); document.addEventListener('keydown', key); return () => { document.removeEventListener('mousedown', outside); document.removeEventListener('keydown', key); }; }, [isMenuOpen]); const searchTab = tabs.find((t) => t.value === 'search'); const openSearch = () => { if (searchTab) onSelectTab(searchTab.value); setIsMenuOpen(false); }; const goHome = () => { setIsMenuOpen(false); onSelectTab('dashboard'); }; return <><header className={`sticky top-0 z-20 px-5 pt-5 pb-3 ${styles.header}`}><div className="flex items-center justify-between gap-3"><button type="button" onClick={goHome} className={`font-display text-2xl font-bold leading-none ${styles.titleButton}`} aria-label="Go to home" title="Home">Mazaosmart</button><button type="button" onClick={openSearch} className="btn-ghost rounded-xl p-2.5 shrink-0" aria-label="Search" title="Search"><Search size={21} strokeWidth={2.25} /></button></div><div className={`text-xs mt-1 truncate ${styles.subtitle}`}>Smart farm records &amp; decisions</div><button onClick={() => window.location.reload()} className="btn-ghost hidden sm:flex rounded-xl px-3 py-2 text-sm items-center gap-2 shrink-0 absolute right-5 top-5" title="Refresh the app. Saved records remain in this browser."><RefreshCw size={15} /> Refresh</button><div className="mt-4 hidden sm:block"><NavTabs tabs={tabs} activeTab={activeTab} onSelect={onSelectTab} /></div></header><nav className={styles.mobileNav} aria-label="Primary navigation">{mobilePrimary.map((t) => { const Icon = t.icon; const active = activeTab === t.value; return <button key={t.value} type="button" data-tour={`nav-${t.value}`} onClick={() => onSelectTab(t.value)} className={`${styles.mobileNavItem} ${active ? styles.mobileNavItemActive : ''}`} aria-current={active ? 'page' : undefined}><Icon size={20} strokeWidth={2.25} /><span>{t.value === 'dashboard' ? 'Home' : t.value === 'log' ? 'Log' : t.value === 'inventory' ? 'Stock' : 'Expenses'}</span></button>; })}<div className={styles.moreMenu} ref={menuRef}><button type="button" data-tour="nav-more" onClick={() => setIsMenuOpen((x) => !x)} aria-haspopup="true" aria-expanded={isMenuOpen} className={`${styles.mobileNavItem} ${mobileMore.some((t) => t.value === activeTab) ? styles.mobileNavItemActive : ''}`}><MoreHorizontal size={20} strokeWidth={2.25} /><span>More</span></button>{isMenuOpen && <div className={styles.morePopover} role="menu">{mobileMore.map((t) => { const Icon = t.icon; return <button key={t.value} data-tour={`more-${t.value}`} type="button" role="menuitem" onClick={() => { setIsMenuOpen(false); onSelectTab(t.value); }} className={styles.moreItem}><Icon size={18} />{t.label}</button>; })}<button type="button" role="menuitem" onClick={() => window.location.reload()} className={styles.moreItem}><RefreshCw size={18} />Refresh</button></div>}</div></nav></>; }
