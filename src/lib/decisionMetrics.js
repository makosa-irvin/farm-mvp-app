export function calculateDecisionMetrics({ production = 0, revenue = 0, cost = 0, feedCost = 0, averageHeadcount = 0, days = 1 }) { const p = Number(production) || 0; const r = Number(revenue) || 0; const c = Number(cost) || 0; const f = Number(feedCost) || 0; const animals = Number(averageHeadcount) || 0; const d = Math.max(1, Number(days) || 1); return { margin: r - c, marginRate: r > 0 ? ((r - c) / r) * 100 : null, costPerProduction: p > 0 ? c / p : null, feedCostPerProduction: p > 0 ? f / p : null, productionPerAnimal: animals > 0 ? p / animals : null, productionPerAnimalPerDay: animals > 0 ? p / (animals * d) : null, feedCostPerAnimal: animals > 0 ? f / animals : null }; }
