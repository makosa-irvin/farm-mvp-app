import { describe, expect, it } from 'vitest'; import { calculateDecisionMetrics } from '../../src/lib/decisionMetrics.js';
describe('decisionMetrics', () => { it('calculates unit economics', () => { const r = calculateDecisionMetrics({ production: 100, revenue: 15000, cost: 9000, feedCost: 5000, averageHeadcount: 20, days: 10 }); expect(r.margin).toBe(6000); expect(r.marginRate).toBe(40); expect(r.feedCostPerProduction).toBe(50); expect(r.productionPerAnimalPerDay).toBe(0.5); }); it('handles zero denominators', () => expect(calculateDecisionMetrics({ production: 0, revenue: 0, cost: 10 }).costPerProduction).toBeNull()); });
