import { describe, expect, it } from 'vitest'; import { clearSearchHistory, readSearchHistory, rememberSearch } from '../../src/lib/searchHistory.js';
describe('searchHistory', () => { const storage = () => ({ data: {}, getItem(k) { return this.data[k] ?? null; }, setItem(k, v) { this.data[k] = v; }, removeItem(k) { delete this.data[k]; } }); it('stores unique recent searches', () => { const s = storage(); rememberSearch('Layer Mash', s); rememberSearch('Supplier', s); rememberSearch('layer mash', s); expect(readSearchHistory(s)).toEqual(['layer mash', 'Supplier']); }); it('clears history', () => { const s = storage(); rememberSearch('feed', s); clearSearchHistory(s); expect(readSearchHistory(s)).toEqual([]); }); });
