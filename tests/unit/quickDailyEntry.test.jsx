import { describe, expect, it, vi } from 'vitest'; import { fireEvent, render, screen } from '@testing-library/react'; import '@testing-library/jest-dom/vitest'; import QuickDailyEntry from '../../src/components/QuickDailyEntry.jsx';
describe('QuickDailyEntry', () => { it('saves production and feed together', () => { const log = vi.fn(); const move = vi.fn(); render(<QuickDailyEntry units={[{ id: 'u1', name: 'Layer House A' }]} inventory={[{ id: 'i1', name: 'Layer Mash', category: 'Feed', unitCost: 75 }]} onAddLog={log} onAddInventoryMove={move} />); fireEvent.change(screen.getByLabelText('Quick produced'), { target: { value: '30' } }); fireEvent.change(screen.getByLabelText('Quick mortality'), { target: { value: '1' } }); fireEvent.change(screen.getByRole('combobox', { name: /feed item/i }), { target: { value: 'i1' } }); fireEvent.change(screen.getByLabelText('Quick feed quantity'), { target: { value: '4.5' } }); fireEvent.click(screen.getByRole('button', { name: /save today's record/i })); expect(log).toHaveBeenCalledWith(expect.objectContaining({ produced: 30, mortality: 1, feedQuantity: 4.5 }), expect.anything()); expect(move).toHaveBeenCalledWith(expect.objectContaining({ itemId: 'i1', quantity: 4.5 })); }); });
