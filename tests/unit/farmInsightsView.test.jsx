import { describe, expect, it, vi } from 'vitest'; import { render, screen } from '@testing-library/react'; import '@testing-library/jest-dom/vitest'; import FarmInsightsView from '../../src/views/FarmInsightsView.jsx';
describe('FarmInsightsView', () => { it('shows decision support sections from real farm data', () => { render(<FarmInsightsView farm={{ units: [{ id: 'u1', name: 'Layer House A', type: 'eggs', initialCount: 20, producePrice: 450, startDate: '2026-08-01' }], logs: [], expenses: [], inventory: [{ id: 'i1', name: 'Layer Mash', category: 'Feed', unit: 'kg', openingStock: 20, reorderLevel: 5, unitCost: 75 }], inventoryMoves: [] }} goTo={vi.fn()} />); expect(screen.getByText('What needs attention?')).toBeInTheDocument(); expect(screen.getByText('Stock runway')).toBeInTheDocument(); expect(screen.getByText('Quick daily entry')).toBeInTheDocument(); expect(screen.getByText('Saved analyses')).toBeInTheDocument(); }); });
